all:
  children:
    swarm_nodes:
      children:
        swarm_masters:
          hosts:
            lyra: null
        swarm_workers:
          hosts:
    storage_nodes:
      hosts:
  hosts:
    node_1:
      administration_ip: "{{ nodes.michel.ip }}"
      ansible_user: "{{ nodes.michel.user }}"
      ansible_host: "{{ administration_ip }}"
      ansible_sudo_pass: "{{ nodes.michel.become_pass }}"
